<dx:ThemedWindow x:Class="ContactAppRX.Views.UpdateContactView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                 xmlns:local="clr-namespace:ContactAppRX.ViewModels" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 Title="Update Contact" Height="400" Width="400"
                 WindowStartupLocation="CenterScreen">


    <Grid>
        <StackPanel Margin="10">
            <dxe:TextEdit Label="First Name" Text="{Binding FirstName}" Width="350" />
            <dxe:TextEdit Label="Last Name" Text="{Binding LastName}" Width="350" />
            <dxe:TextEdit Label="Phone Number" Text="{Binding PhoneNumber}" Width="350" />
            <dxe:TextEdit Label="Email" Text="{Binding Email}" Width="350" />
            <dxe:TextEdit Label="Address" Text="{Binding Address}" Width="350" />
            <dxe:CheckEdit IsChecked="{Binding IsFavorite}" Content="Favorite" />
            <dx:SimpleButton Content="Save" Command="{Binding SaveCommand}" Width="350" Margin="10,20,0,0" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</dx:ThemedWindow> 
